{
  "name": "blog-salish-dev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run generate-pdf",
    "build:ci": "astro build",
    "generate-pdf": "wrangler pages dev ./dist --port 4321 & sleep 3 && node scripts/generate-pdf.js && kill $!",
    "preview": "astro build && wrangler pages dev",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.0",
    "@astrojs/mdx": "^4.3.1",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.4.2",
    "astro": "^5.12.6",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "puppeteer": "^24.15.0",
    "wrangler": "^4.26.1"
  }
}
